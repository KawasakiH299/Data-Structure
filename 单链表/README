1，通常来说"a is b"，如果是True的情况下，a和b的内存地址相同，而"==",是在值相同的情况下而返回True
2，a = 10  此时a的值是数值10的内存地址
3，demon01中的self.__OneNode 一直指的都是链表中的第一个node
4,冒号是参数的类型建议符，告诉程序员希望传入的实参的类型。
5,函数后面跟着的箭头是函数返回值的类型建议符，用来说明该函数返回的值是什么类型